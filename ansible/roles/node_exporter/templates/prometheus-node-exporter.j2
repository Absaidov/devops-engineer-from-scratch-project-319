# Managed by Ansible. Local changes will be overwritten.
ARGS="--web.listen-address={{ node_exporter_listen_address }}:{{ node_exporter_port }}{% for collector in node_exporter_enabled_collectors %} --collector.{{ collector }}{% endfor %}{% for argument in node_exporter_extra_arguments %} {{ argument }}{% endfor %}"
